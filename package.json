{
  "name": "phoenix-tech-refresh-executive-preview",
  "version": "0.2.0",
  "private": true,
  "description": "Phoenix Tech Refresh executive website and original interactive Phoenix Flow background.",
  "type": "module",
  "scripts": {
    "start": "node build.mjs && node serve.mjs",
    "build": "node build.mjs",
    "standalone": "node build.mjs --standalone",
    "check": "node --check app.js && node --check content.js && node --check icons.js && node --check motion.js && node --check build.mjs && node --check serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
