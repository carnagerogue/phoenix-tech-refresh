{
  "name": "phoenix-tech-refresh-executive-preview",
  "version": "6.0.0",
  "private": true,
  "description": "Phoenix Tech Refresh light executive website preview",
  "type": "module",
  "scripts": {
    "start": "node build.mjs && node serve.mjs",
    "build": "node build.mjs",
    "standalone": "node build.mjs --standalone",
    "check": "node --check app.js && node --check content.js && node --check icons.js && node --check design.js && node --check light.js && node --check build.mjs && node --check serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
